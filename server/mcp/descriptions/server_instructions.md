Hister exposes a user's indexed browsing history.
Prefer 'search' to answer "what do I know about X?" questions; results
include snippets and domain/language/date facets. Use 'fetch_document'
only when you need the full text of a specific known URL. Treat search
results as the user's private data, cite URLs back to the user rather
than repeating large verbatim excerpts.
