# Hister documentation

Use [hugo](https://gohugo.io/) to build the site.

Run `hugo server` for development and `hugo build` to build the static website.

## Files

Configuration: `./hugo.toml`

Documentation: `./content/documentation`

Posts: `./content/posts`

Index page: `./layouts/home.html`
