export const prerender = true;

export function entries() {
  return [
    { slug: 'getting-started' },
    { slug: 'server-setup' },
    { slug: 'query-language' },
    { slug: 'configuration' }
  ];
}